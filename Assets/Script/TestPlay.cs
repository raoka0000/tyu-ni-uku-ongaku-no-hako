using UnityEngine;
using System.Collections;
using System.Runtime.InteropServices;


public class TestPlay : MonoBehaviour {


	public void ButtonPush() {
		Debug.Log("Button Push !!");


		//Binding.Initialize ();
		//AudioSource extraAudioSource= GetComponent<AudioSource>();
		//WWW wwwFile;
		//wwwFile = new WWW("file://" + Application.persistentDataPath + ".wav");
		//extraAudioSource.clip = wwwFile.GetAudioClip(false, false, AudioType.WAV);


		//AudioSource extraAudioSource= GetComponent<AudioSource>();


		//WWW wwwFile;
		//wwwFile = new WWW("file://" + Application.persistentDataPath + valueForProperty:MPMediaItemPropertyTitle + ".wav");
		//extraAudioSource.clip = wwwFile.GetAudioClip(false, false, AudioType.WAV);

	}

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
